version: 2
jobs:
  build:
    docker:
      - image: circleci/node:9.6.1
    steps:
      - checkout
      - run: sudo npm install truffle
      - run: sudo npm install ganache-cli
      - run: ganache-cli & truffle test
